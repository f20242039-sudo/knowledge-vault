# Knowledge Vault — Python dependencies

# UI
streamlit>=1.35.0

# AI — Groq API (cloud / BYOK)
groq>=0.9.0

# AI — Ollama (local inference)
requests>=2.31.0   # used for Ollama HTTP calls (usually already installed)

# Embeddings (runs locally, no API needed)
sentence-transformers>=3.0.0

# Database
supabase>=2.4.0

# File parsing
PyMuPDF>=1.24.0        # PDF extraction (imported as fitz)
python-docx>=1.1.0     # DOCX extraction

# Data validation
pydantic>=2.7.0

# Config / secrets
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
